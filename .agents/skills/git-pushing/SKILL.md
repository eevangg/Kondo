---
name: git-pushing
description: Safely stage, commit, and push codebase milestones.
---

# Git Pushing Skill

## Workflow
- Verify git working tree status (`git status`).
- Exclude secrets, `.env` files, and local build artifacts (`node_modules`, `dist`).
- Create descriptive, structured commit messages (`feat: add shared expense settlement logic`).
